PACKAGE_NAME="zenbook-duo-hid-asus"
PACKAGE_VERSION="0.1.0"

BUILT_MODULE_NAME[0]="hid-asus"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"

MAKE[0]="make KERNEL_VERSION=${kernelver}"
CLEAN="make KERNEL_VERSION=${kernelver} clean"
